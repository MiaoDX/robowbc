---
phase: 03-expose-a-live-pyo3-mujoco-session-for-direct-roboharness-bac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/robowbc-sim/src/transport.rs
  - crates/robowbc-py/Cargo.toml
  - crates/robowbc-py/src/lib.rs
  - docs/python-sdk.md
  - examples/python/roboharness_backend.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "The Python SDK exposes a live MuJoCo session with reset, step, get_state, save_state, restore_state, capture_camera, and get_sim_time equivalents."
    - "The live session keeps policy inference and MuJoCo stepping in Rust instead of reimplementing the control loop in Python."
    - "A thin roboharness adapter path exists without adding roboharness as a Rust dependency."
  artifacts:
    - path: "crates/robowbc-py/src/lib.rs"
      provides: "User-facing Python MuJoCo session API."
      min_lines: 100
    - path: "crates/robowbc-sim/src/transport.rs"
      provides: "Snapshot/restore/render/state helpers used by the Python session."
      min_lines: 50
    - path: "examples/python/roboharness_backend.py"
      provides: "Reference roboharness adapter for the live session."
      min_lines: 30
  key_links:
    - from: "crates/robowbc-py/src/lib.rs"
      to: "crates/robowbc-sim/src/transport.rs"
      via: "Rust-owned sim/session helpers"
      pattern: "Mujoco"
    - from: "examples/python/roboharness_backend.py"
      to: "crates/robowbc-py/src/lib.rs"
      via: "Python adapter over live session API"
      pattern: "Session|camera"
---

<objective>
Expose a live Python-facing MuJoCo session that lets roboharness attach to the
existing Rust simulation path.

Purpose: Replace adapter-level replay-only integration with a narrow live
backend seam when replay is no longer the right fit, without moving control
semantics into Python.
Output: transport helper methods, a Python session class, adapter example, and
SDK documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONTEXT.md
@.planning/phases/03-expose-a-live-pyo3-mujoco-session-for-direct-roboharness-bac/03-CONTEXT.md
@crates/robowbc-py/src/lib.rs
@crates/robowbc-sim/src/transport.rs
@crates/robowbc-comm/src/lib.rs
@docs/python-sdk.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live-session helpers to MujocoTransport</name>
  <files>crates/robowbc-sim/src/transport.rs</files>
  <read_first>crates/robowbc-sim/src/transport.rs</read_first>
  <action>Add transport methods for reset, state snapshot/restore, current sim time, and RGB camera capture so higher layers can expose a live session without duplicating raw MuJoCo logic. Reuse MuJoCo full-physics state copy where possible and keep helper methods small and testable.</action>
  <verify>cargo test -p robowbc-sim -- --nocapture</verify>
  <acceptance_criteria>
    - `MujocoTransport` exposes reset/state/save/restore/time helpers
    - helper methods preserve MuJoCo-owned physics state rather than partial ad-hoc copies
    - camera capture is available through a stable Rust API
  </acceptance_criteria>
  <done>The simulation crate provides the primitives needed for a live Python session</done>
</task>

<task type="auto">
  <name>Task 2: Expose a Python-facing MuJoCo session in robowbc-py</name>
  <files>crates/robowbc-py/Cargo.toml, crates/robowbc-py/src/lib.rs</files>
  <read_first>crates/robowbc-py/Cargo.toml, crates/robowbc-py/src/lib.rs, crates/robowbc-comm/src/lib.rs</read_first>
  <action>Add the necessary sim/numpy dependencies to `robowbc-py`, then implement a session class that loads a full RoboWBC config, owns a policy plus `MujocoTransport`, and exposes `reset`, `step`, `get_state`, `save_state`, `restore_state`, `capture_camera`, and `get_sim_time`. Keep `step` Rust-owned by gathering live sim observations and invoking the policy before advancing MuJoCo.</action>
  <verify>cargo check --manifest-path crates/robowbc-py/Cargo.toml</verify>
  <acceptance_criteria>
    - the Python module exports a live session class
    - `step` advances the Rust-owned policy+MuJoCo loop, not a Python reimplementation
    - state and camera payloads are Python-friendly enough for an adapter layer
  </acceptance_criteria>
  <done>The Python SDK can host a live RoboWBC MuJoCo session directly</done>
</task>

<task type="auto">
  <name>Task 3: Provide a roboharness adapter example and SDK docs</name>
  <files>examples/python/roboharness_backend.py, docs/python-sdk.md</files>
  <read_first>docs/python-sdk.md, /home/mi/ws/gogo/roboharness/src/roboharness/core/harness.py</read_first>
  <action>Add a small Python example that adapts the new session to roboharness's `SimulatorBackend` expectations and update the SDK docs with session usage, build notes, and the live-backend integration path.</action>
  <verify>python3 -m py_compile examples/python/roboharness_backend.py</verify>
  <acceptance_criteria>
    - the example shows how to wrap the session for roboharness
    - the SDK docs explain the session API and its ownership boundary
    - no roboharness Rust dependency is introduced
  </acceptance_criteria>
  <done>There is a clear documented path from RoboWBC Python session to roboharness backend usage</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p robowbc-sim` passes
- [ ] `cargo check --manifest-path crates/robowbc-py/Cargo.toml` passes
- [ ] `python3 -m py_compile examples/python/roboharness_backend.py` passes
</verification>

<success_criteria>
- All tasks completed
- Python SDK exposes the required live-session methods
- Control ownership stays in Rust
- roboharness integration path is demonstrated without hard Rust dependency coupling
</success_criteria>

<output>
After completion, create `.planning/phases/03-expose-a-live-pyo3-mujoco-session-for-direct-roboharness-bac/03-01-SUMMARY.md`
</output>
