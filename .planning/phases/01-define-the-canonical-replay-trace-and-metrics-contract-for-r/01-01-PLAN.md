---
phase: 01-define-the-canonical-replay-trace-and-metrics-contract-for-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/robowbc-cli/src/main.rs
  - crates/robowbc-sim/src/transport.rs
  - scripts/roboharness_report.py
  - docs/roboharness-integration.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "RoboWBC emits a canonical replay-state artifact that is no longer limited by report.max_frames."
    - "The replay artifact carries enough MuJoCo state to reconstruct floating-base motion truthfully."
    - "roboharness replay consumes canonical replay state for rendering while keeping online runtime metrics authoritative."
  artifacts:
    - path: "crates/robowbc-cli/src/main.rs"
      provides: "Replay-trace serialization separate from the capped run report sample."
      min_lines: 50
    - path: "scripts/roboharness_report.py"
      provides: "Replay path that prefers the canonical trace and restores floating-base state."
      min_lines: 50
    - path: "docs/roboharness-integration.md"
      provides: "Documented artifact contract describing replay-vs-metrics truth boundaries."
      min_lines: 20
  key_links:
    - from: "crates/robowbc-cli/src/main.rs"
      to: "scripts/roboharness_report.py"
      via: "canonical replay artifact path and schema"
      pattern: "replay"
    - from: "crates/robowbc-sim/src/transport.rs"
      to: "scripts/roboharness_report.py"
      via: "floating-base or qpos/qvel replay state"
      pattern: "qpos|floating"
---

<objective>
Define and implement the canonical replay-state artifact contract for
roboharness-facing MuJoCo runs.

Purpose: Make visual replay truthful without changing the existing rule that
online runtime metrics are authoritative.
Output: CLI replay artifact support, roboharness replay consumption, and
updated integration docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONTEXT.md
@.planning/phases/01-define-the-canonical-replay-trace-and-metrics-contract-for-r/01-CONTEXT.md
@crates/robowbc-cli/src/main.rs
@crates/robowbc-sim/src/transport.rs
@scripts/roboharness_report.py
@docs/roboharness-integration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit a canonical replay-state artifact from the CLI</name>
  <files>crates/robowbc-cli/src/main.rs, crates/robowbc-sim/src/transport.rs</files>
  <read_first>crates/robowbc-cli/src/main.rs, crates/robowbc-sim/src/transport.rs</read_first>
  <action>Add a replay-trace payload that is written alongside `run_report.json` when report output is enabled. Keep `RunReport` intact for benchmark consumers, but add a second artifact that captures all control ticks, sim metadata, and MuJoCo replay state. For MuJoCo-backed runs, persist `qpos` and `qvel` per tick plus loaded model metadata; for non-sim runs, persist the best available joint/base-pose fallback. Do not route authoritative timing metrics through the replay artifact.</action>
  <verify>cargo test -p robowbc-cli -- --nocapture</verify>
  <acceptance_criteria>
    - `crates/robowbc-cli/src/main.rs` writes a replay artifact that is not capped by `report.max_frames`
    - the replay artifact includes versioned metadata and per-tick replay state
    - `run_report.json` remains readable by existing benchmark tooling
  </acceptance_criteria>
  <done>CLI produces both an authoritative run report and a separate canonical replay-state artifact</done>
</task>

<task type="auto">
  <name>Task 2: Switch roboharness replay to the canonical trace</name>
  <files>scripts/roboharness_report.py</files>
  <read_first>scripts/roboharness_report.py, crates/robowbc-cli/src/main.rs</read_first>
  <action>Update the replay script so it prefers the canonical replay artifact when present, falls back to `run_report.json.frames` for backward compatibility, and restores floating-base motion from recorded state instead of forcing the upright default transform. Keep the generated HTML metrics sourced from the authoritative online report, not replay-derived recomputation.</action>
  <verify>python3 -m py_compile scripts/roboharness_report.py</verify>
  <acceptance_criteria>
    - `scripts/roboharness_report.py` looks for the canonical replay artifact before using `run_report.json.frames`
    - floating-base state is restored from recorded data instead of a hardcoded default
    - report summary metrics still come from `run_report.json`
  </acceptance_criteria>
  <done>roboharness replay renders from truthful replay state while preserving online metric authority</done>
</task>

<task type="auto">
  <name>Task 3: Document the artifact contract and truth boundary</name>
  <files>docs/roboharness-integration.md</files>
  <read_first>docs/roboharness-integration.md, .planning/CONTEXT.md</read_first>
  <action>Revise the integration guide so it explicitly documents the two artifact classes: authoritative runtime metrics in `run_report.json` and canonical replay state in the new replay artifact. Call out that long-run replay no longer depends on the capped `frames` sample and describe what data is stored for faithful MuJoCo reconstruction.</action>
  <verify>rg -n \"canonical replay|authoritative runtime metrics|floating-base\" docs/roboharness-integration.md</verify>
  <acceptance_criteria>
    - the guide names both artifact classes and their responsibilities
    - the guide explains that replay is visual evidence, not the source of truth for runtime metrics
    - the guide documents floating-base-aware replay behavior
  </acceptance_criteria>
  <done>Users can understand the new artifact contract without reading the source code</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p robowbc-cli` passes
- [ ] `python3 -m py_compile scripts/roboharness_report.py` passes
- [ ] replay artifact docs match the implemented file names and responsibilities
</verification>

<success_criteria>
- All tasks completed
- Replay-state artifact is separate from the capped report sample
- MuJoCo replay can recover floating-base motion from recorded state
- Documentation explains replay truth boundaries clearly
</success_criteria>

<output>
After completion, create `.planning/phases/01-define-the-canonical-replay-trace-and-metrics-contract-for-r/01-01-SUMMARY.md`
</output>
