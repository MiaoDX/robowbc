# Testing

Generated on 2026-04-27 from the current codebase, CI config, and local
verification commands.

## Current Validation Results

Commands run for this scan:

| Command | Result |
| --- | --- |
| `cargo build` | passed |
| `cargo check` | passed |
| `cargo test` | passed |
| `cargo clippy -- -D warnings` | passed |
| `cargo fmt --check` | passed |
| `cargo doc --no-deps` | passed |
| `python3 -m pytest -q` | passed: 42 tests, 5 subtests |

Rust test totals from the `cargo test` run:

- 172 passed
- 12 ignored
- 0 failed

The local validation picture is good. The current tree is not just buildable;
it also passes the main Rust and Python test surfaces.

## Test Surfaces by Layer

### Rust crates

The Rust suite is mostly embedded directly in crate sources and exercises:

- core contract validation in `robowbc-core`
- registry discovery and build paths in `robowbc-registry`
- transport loop timing, wire encoding, and zenoh behavior in `robowbc-comm`
- MuJoCo config and transport behavior in `robowbc-sim`
- scene parsing and config round-trips in `robowbc-vis`
- ONNX backend, wrapper contracts, and multiple policy-specific paths in
  `robowbc-ort`
- CLI config parsing, runtime command shaping, report writing, and synthetic
  loop execution in `robowbc-cli`
- PyO3-backed user model loading in `robowbc-pyo3`

Current inventory:

- 198 Rust `#[test]` functions under `crates/`
- 2 Criterion bench targets:
  `crates/robowbc-comm/benches/control_loop.rs` and
  `crates/robowbc-ort/benches/inference.rs`

### Python/reporting layer

The standalone Python suite in `tests/` covers:

- benchmark normalization and summary rendering
- policy showcase generation
- roboharness report helpers
- site browser smoke helpers
- generated site bundle validation

Current inventory:

- 5 Python test modules
- local run result: `42 passed, 5 subtests passed`

This is important because a meaningful part of the user-visible product is
generated by Python scripts rather than Rust binaries alone.

## Ignored-Test Categories

The 12 ignored Rust tests are informative, not suspicious. They cluster into
four explicit categories:

1. Real model assets not downloaded locally yet.
2. User-provided checkpoints that public upstreams do not ship.
3. Optional local Torch installation for one PyO3 path.
4. A zenoh loopback test that requires an available peer.

Concrete ignored examples:

- GEAR-Sonic real-model inference
- Decoupled WBC real-model inference
- WBC-AGILE real-model inference
- BFM-Zero real-model inference
- HOVER real-model inference
- WholeBodyVLA real-model inference
- Unitree zenoh loopback
- Torch-backed PyO3 inference

Interpretation:

- contract-level and synthetic-path coverage is strong
- production-like coverage exists, but part of it is opt-in and
  environment-dependent

## CI Coverage Shape

The GitHub Actions workflow splits validation across dedicated jobs.

### `rust` job

Runs:

- `make check`
- `make test`
- `make sim-feature-test`
- `make clippy`
- `make fmt-check`

### `docs` job

Runs:

- `make rust-doc`
- `make docs-book`

### `showcase` job

Runs:

- headless MuJoCo EGL dependency install
- `make showcase-verify`
- artifact upload for benchmark and site bundles

### `python-sdk` job

Runs:

- `make python-sdk-verify`

### Publishing jobs

- Pages deploys the showcase on pushes to `main`
- PyPI publishing builds wheels and sdist on version tags

## What CI Does Not Explicitly Show

There is no explicit `pytest` invocation in:

- `.github/workflows/ci.yml`
- `Makefile`
- `README.md`

That does not mean the Python layer is unvalidated. The showcase and bundle
verification paths do exercise Python code. But it does mean the dedicated
`tests/` suite is a separate test surface and is not obviously wired into CI by
name.

## Quality Signals

Strong signals:

- Rust compile, test, lint, format, and docs all pass locally
- Python artifact/report tests pass locally
- CI validates not only library behavior but also the full showcase bundle and
  Python SDK packaging path
- benchmark and site generation are treated as testable artifacts

Partial signals:

- some of the most realistic policy tests are ignored until assets or external
  peers are present
- the strongest user-facing HTML/report pipeline is validated by command-level
  workflows rather than a single small unit-test surface

## Readout

- The repo has strong contract and integration testing for a project of this
  size.
- The most realistic model and hardware-adjacent checks are present but not all
  runnable by default.
- The reporting and publishing layer is materially tested, which matches the
  product shape of the repo.
- The one testing ambiguity worth noting is CI discoverability for the
  standalone Python `tests/` suite.
